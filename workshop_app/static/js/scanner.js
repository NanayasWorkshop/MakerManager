/**
 * Workshop Management System - Scanner Functionality
 * Main entry point for the scanner component - redirects to inline code
 */

// The scanner is now implemented directly in the HTML template
console.log("Scanner module loaded - using inline implementation");
